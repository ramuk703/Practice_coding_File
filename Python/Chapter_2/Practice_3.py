# Write a program to count the number of students with the "A" grade in the following tuple.
grade = ["a","A","b","b","A","A","b","C"]
gra = grade.count("A")
print(gra)

# Store the abovfe values in a list & sort them from "A" to "D"
grade.sort()
print(grade)